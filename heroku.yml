# heroku stack:set container --app stijanic-docker
# git push heroku master
# heroku ps
# heroku ps:scale web=1
# heroku ps:kill release

build:
  docker:
    web: Dockerfile
run:
  web: node node.js/4.2.4/_Node.js/learnyounode/http-json-api-server.js 8080
